import Layout from './components/composed/Layout/Layout';

const App = (): React.ReactElement => {

  return (
      <Layout>
        <span> Poioio</span>  
      </Layout>
  );
}

export default App;
